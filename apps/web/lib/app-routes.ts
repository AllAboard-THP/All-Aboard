/** Canonical home for authenticated product navigation. */
export const APP_HOME_PATH = "/feed";

/** Default post-auth redirect when no returnTo is provided. */
export const DEFAULT_POST_LOGIN_PATH = APP_HOME_PATH;
