import { test } from "@playwright/test";

test.describe("R5 — Messages", () => {
  test("liste + conversation + chat React", async () => {
    test.skip(
      true,
      "Routes messages / chat non livrées — activer quand §8.2 / §8.7 messages existe.",
    );
  });
});
