import { test } from "@playwright/test";

test.describe("R3 — CGU", () => {
  test("modale CGU : case et validation", async () => {
    test.skip(
      true,
      "Modale CGU non branchée sur apps/web — retirer ce skip quand le flux §8.7 est livré.",
    );
  });
});
